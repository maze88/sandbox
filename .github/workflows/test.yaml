name: Sandbox

on:
  push:
    branches:
    - "main"
    - "v*.x"
    tags:
    - "v*"

env:
  DEFAULT_BRANCH: main

jobs:
  sandbox:
    if: ${{ startsWith(github.event.head_commit.message, 'foo:') }}
    runs-on: ubuntu-latest
    steps:
    # - uses: actions/checkout@v4
    - run: |
        echo yay
